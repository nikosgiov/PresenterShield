<Application x:Class="PresenterShield.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PresenterShield"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             StartupUri="Views/MainWindow.xaml">
    <Application.Resources>
        <ContextMenu x:Key="TrayMenu">
            <MenuItem Header="Show PresenterShield" Click="ShowMainWindow_Click" />
            <MenuItem Header="Exit" Click="Exit_Click" />
        </ContextMenu>

        <tb:TaskbarIcon x:Key="NotifyIcon"
                        ToolTipText="PresenterShield"
                        IconSource="pack://application:,,,/Assets/presenterShield.png"
                        ContextMenu="{StaticResource TrayMenu}">
        </tb:TaskbarIcon>
    </Application.Resources>
</Application>
